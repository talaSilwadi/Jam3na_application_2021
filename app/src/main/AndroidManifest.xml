<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jam3na_testing">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jam3na_Testing">
        <activity
            android:name=".View.forgetpasswordActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            />
        <activity
            android:name=".View.LoginController"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.MainActivity" >

        </activity>
        <activity
            android:name=".View.RegisterController"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />

        <activity android:name=".View.profileController" />
        <activity android:name=".View.CreateGroupController" >

        </activity>

    </application>

</manifest>